// Gerado automáticamente
import { BaseController } from '@controllers';
import {{tablenameCapital}}Model from './model';
import { {{tablenameCapital}}Entity } from './entity';
import { QueryFilter } from '@app-types';

export class {{tablenameCapital}}Controller extends BaseController<{{tablenameCapital}}Entity> {
  constructor() {
      super({{tablenameCapital}}Model);
  }

  // Filtro padrão do findAll e find - inserido na pesquisa caso não exista outro filtro ou where
  public getDefaultFilters(): QueryFilter[] {
    return []
  }

  /**
	 * Alterar o discovery da entidade
	 */
	// public async discovery(): Promise<IDiscoveryEntity> {
	// 	return {
	// 		resource: this.getModelTable(),
	// 		capabilities: {
	// 			grpc: this.enableGrpc,
  //      rest: this.enableRest,
	// 			auth: ['bearer']
	// 		},
	// 		methods: ['GET', 'POST', 'PATCH', 'DELETE']
	// 	}
	// }
}
